package io.isometrik.ui.messages.media.audio.record.listeners;

import android.view.View;

/**
 * The interface On record click listener.
 */
public interface OnRecordClickListener {
  /**
   * On click.
   *
   * @param v the v
   */
  void onClick(View v);
}